// Este archivo era para Prisma v7+ (Prisma Postgres local).
// Este proyecto usa Prisma v5.22.0 con Supabase (PostgreSQL estándar).
// La configuración está en prisma/schema.prisma y la URL en .env (DATABASE_URL).
// Este archivo puede ignorarse o eliminarse con seguridad.

